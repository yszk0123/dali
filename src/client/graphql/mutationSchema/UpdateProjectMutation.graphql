#import "../querySchema/ProjectItem.graphql"

mutation UpdateProject($projectId: ID!, $title: String!) {
  updateProject(projectId: $projectId, title: $title) {
    id
    title
    ...ProjectItem_project
  }
}
