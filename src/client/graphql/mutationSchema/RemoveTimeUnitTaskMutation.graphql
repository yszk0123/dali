#import "../querySchema/TimeUnitTaskItem_task.graphql"

mutation RemoveTimeUnitTask($taskId: ID!, $timeUnitId: ID) {
  task: updateTask(taskId: $taskId, timeUnitId: $timeUnitId) {
    id
    ...TimeUnitTaskItem_task
  }
}
