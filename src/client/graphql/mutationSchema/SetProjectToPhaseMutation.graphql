#import "../querySchema/PhaseItem_phase.graphql"

mutation SetProjectToPhase($phaseId: ID!, $projectId: ID!, $taskDone: Boolean) {
  setProjectToPhase(phaseId: $phaseId, projectId: $projectId) {
    id
    ...PhaseItem_phase
  }
}
