#import "../querySchema/PhaseTaskItem.graphql"

mutation MoveTaskToPhase($taskId: ID!, $phaseId: ID!) {
  moveTaskToPhase(taskId: $taskId, phaseId: $phaseId) {
    task {
      id
      ...PhaseTaskItem_task
    }
    sourcePhase {
      id
    }
    targetPhase {
      id
    }
  }
}
