import styled from 'styled-components';

const Button = styled.span`
  margin: 3px;
  padding: 0.7rem;
  border-radius: 4px;
  background: #13c;
  color: #eee;
  cursor: pointer;
`;

export default Button;
